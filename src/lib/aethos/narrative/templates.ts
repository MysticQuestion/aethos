export const NARRATIVE_TEMPLATES = {
  timingBrief:
    "This timing window may be useful for observation. Treat the theme as a reflective context marker, not a command.",
  calibration:
    "Aethos compares journal check-ins against timing windows only after repeated entries create a personal baseline."
} as const;
