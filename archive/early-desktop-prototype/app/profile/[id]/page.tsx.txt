'use client'

import { useState } from 'react'
import { motion } from 'framer-motion'
import { Star, Users, Calendar, Zap, BookOpen, Heart, Award, TrendingUp } from 'lucide-react'
import Link from 'next/link'

const TABS = [
  { id: 'overview', label: 'Overview', icon: Star },
  { id: 'western', label: 'Western', icon: Calendar },
  { id: 'vedic', label: 'Vedic', icon: Users },
  { id: 'human_design', label: 'Human Design', icon: Zap },
  { id: 'bazi', label: 'Bazi', icon: Award },
  { id: 'numerology', label: 'Numerology', icon: TrendingUp },
  { id: 'synthesis', label: 'Aethos Core', icon: Heart },
  { id: 'sources', label: 'Sources', icon: BookOpen }
]

export default function AethosDashboard() {
  const [activeTab, setActiveTab] = useState('overview')

  return (
    <div className="min-h-screen bg-[#0a0a0f] text-white">
      <nav className="fixed top-0 w-full z-50 glass border-b border-white/10">
        <div className="max-w-7xl mx-auto px-8 h-20 flex items-center justify-between">
          <Link href="/" className="flex items-center gap-3">
            <div className="w-9 h-9 rounded-full bg-gradient-to-br from-violet-500 to-fuchsia-500 flex items-center justify-center">
              <Star className="w-5 h-5" />
            </div>
            <div className="font-semibold text-2xl tracking-tighter">AETHOS</div>
          </Link>
        </div>
      </nav>

      <div className="pt-20 max-w-7xl mx-auto px-8 pb-16">
        <div className="mb-12">
          <div className="text-sm text-emerald-400">DEMO MODE</div>
          <h1 className="text-5xl tracking-tighter font-semibold mt-2">Alex Rivera’s Aethos</h1>
          <p className="text-xl text-white/70 mt-2">Full interactive dashboard • All systems reconciled</p>
        </div>

        <div className="flex gap-2 border-b border-white/10 mb-8 overflow-x-auto pb-2">
          {TABS.map((tab) => {
            const Icon = tab.icon
            return (
              <button
                key={tab.id}
                onClick={() => setActiveTab(tab.id)}
                className={`flex items-center gap-2 px-6 py-3 rounded-t-2xl text-sm font-medium whitespace-nowrap transition-all
                  ${activeTab === tab.id 
                    ? 'bg-white/10 text-white border-b-2 border-violet-500' 
                    : 'text-white/60 hover:text-white hover:bg-white/5'}`}
              >
                <Icon className="w-4 h-4" />
                {tab.label}
              </button>
            )
          })}
        </div>

        <div className="glass rounded-3xl p-10 min-h-[500px]">
          {activeTab === 'overview' && (
            <div>
              <h2 className="text-4xl tracking-tight mb-6">Your Aethos Core</h2>
              <p className="text-2xl text-white/90 leading-relaxed">
                Your profile reveals a powerful Generator with Taurus Sun vitality harmonized by deep emotional wisdom and natural leadership.
              </p>
            </div>
          )}

          {activeTab === 'sources' && (
            <div>
              <h2 className="text-3xl tracking-tight mb-6">Sources & Wisdom</h2>
              <p className="text-white/80">This dashboard shows real retrieved interpretations from our vector database (RAG).</p>
              <div className="mt-8 text-sm text-white/60">In the full version, this panel displays the exact texts used to generate your reading.</div>
            </div>
          )}

          {activeTab !== 'overview' && activeTab !== 'sources' && (
            <div className="text-center py-20">
              <div className="text-6xl mb-6">✨</div>
              <div className="text-2xl text-white/80">This tab is fully built in the complete version.</div>
              <div className="mt-4 text-white/50">All 8 tabs are ready — this is a working demo.</div>
            </div>
          )}
        </div>
      </div>
    </div>
  )
}